{
  "mcpServers": {
    "agent-rtc": {
      "command": "node",
      "args": ["dist/redis-channel.js"],
      "env": {
        "REDIS_URL": "redis://localhost:6379",
        "AGENT_NAME": "${AGENT_NAME:-Agent}",
        "IS_MASTER": "${IS_MASTER:-false}"
      }
    }
  }
}
